const fs = require('fs');
const path = require('path');
const zlib = require('zlib');

// Since we have canvas or sharp or jimp? Let's check or write a pure node / powershell script.
console.log("Node version", process.version);
